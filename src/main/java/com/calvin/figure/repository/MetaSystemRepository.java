package com.calvin.figure.repository;

import com.calvin.figure.entity.MetaSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MetaSystemRepository extends JpaRepository<MetaSystem, Integer> {
}
